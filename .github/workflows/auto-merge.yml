name: Auto-merge migration branch
on:
  push:
    branches:
      - 'claude/fix-kennion-database-instance-ZThAG'

permissions:
  contents: write
  pull-requests: write

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main
          token: ${{ github.token }}

      - name: Merge branch into main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin claude/fix-kennion-database-instance-ZThAG
          git merge origin/claude/fix-kennion-database-instance-ZThAG --no-edit -m "Merge: Migrate data from old Postgres to new instance"
          git push origin main
